# HANDOFF: Claude → Codex

- 対象リポジトリ: `wuthering-trends-web`
- 期間: 2026-08-11 〜 2026-08-14
- 対象コミット: `fd72a15`（セッション開始時点）〜 `e3a7b97`（現在の main）
- 状態: **全て main にマージ済み・本番反映済み**。未コミットの変更なし

---

## 1. 作業目的

3つの目的で作業した。

1. **台本作成システム（別プロジェクト script_forge）との連携**
   2つのNotion DBを横断した「ピックアップ（採用）」の運用を成立させる。
   本アプリは第3段階（ピックアップ済みの一覧表示と解除）を担当する。
2. **収集精度の是正**
   除外フィルタの誤爆と、沈黙していた情報源の発見・修復。
3. **カレンダーの拡充**
   競合タイトル（原神/スタレ/ゼンゼロ/NTE/エンドフィールド）のアップデート日程を、
   鳴潮の予定と同じカレンダーで見られるようにする。

---

## 2. 実施内容

### 2-1. ピックアップ連携（新機能）

| 追加 | 内容 |
|---|---|
| `/pickups` 画面 | 両DBの `採用=true` を横断表示。単体／複数選択／一括で解除 |
| `GET /api/pickups` | 両DBを全ページ取得し、動画IDで名寄せして返す |
| `PATCH /api/pickups` | `{ids, adopted, status}` で採用・制作状況を更新 |
| YouTube動画DB | `採用`(checkbox) と `制作状況`(select) を自動追加。企画DBと同一の列名・選択肢 |

- 解除は `採用` を false にするだけ。**行の削除・アーカイブは一切しない**
- 名寄せで束ねた相手のページIDは `linkedIds` で返し、解除時は全行まとめて外す
- Notion のレート制限（毎秒3件前後）に合わせ、PATCH は3件ずつ 350ms 間隔

### 2-2. データ消失につながる不具合の修正（3件）

いずれも既存コードの欠陥。**放置すると人の判断結果が黙って消える**。

1. **重複チェックが直近100行しか見ていなかった**
   Deduplication Shield が `page_size:100` の1回だけで打ち切っており、
   DBが100行を超えた時点でそれ以前が「無かったこと」になっていた。
   実測で企画DB 518行のうちユニークな動画は122本のみ。全ページ走査に修正。

2. **品質パトロールが採用済みカードを消し得た**
   Phase 5-B が `採用` を見ずに全行を検査していた。対象外にした。

3. **7日自動アーカイブが「制作中」「投稿済み」「見送り」を消していた**
   採用を外しても制作状況は記録として残るため、「画面上は制作中なのに7日で消える」
   状態だった。掃除対象を次の条件に変更した。

   ```
   作成7日経過 かつ 採用=false
   かつ 制作状況が 制作中・投稿済み・見送り のいずれでもない
   ```

   実質、未着手（と未設定）だけが消える。

   **「見送り」の永久保持はオーナー判断**。重複チェックは「今Notionに残っている行」の
   URLで判定するため、行を消すと収集側がその記事を忘れて再収集し、同じネタを
   何度も却下させることになる。行そのものが「一度断った」という記録として機能している。

### 2-3. 除外フィルタの是正

**部分一致の誤爆を修正した。**

| 語 | 巻き込んでいたもの | 対応 |
|---|---|---|
| `レイヤー` | **`プレイヤー` に一致。** 「プレイヤースキル次第で最強火力が出るキャラ3選」等 実測9件 | 削除 |
| `写真集` | ゲーム内スクショ集（正当なネタ） | 削除 |
| `崩壊` | 一般語。「バランス崩壊」等を巻き込みうる | `崩壊3rd` / `崩壊スターレイル` へ絞り込み |
| `美女` `美脚` | コスプレ語と重複、かつキャラランキング系を巻き込む恐れ | 削除 |

**追加した語**（台本作成側と突き合わせて確定）:
コスプレ / cosplay / フィギュア / ワンフェス / ワンダーフェスティバル /
描いてみた / 落書き / らくがき / 一発描き / だけで描いた / 絵師 /
ファンアート / fanart / イラストメイキング / 阿波踊り / 鳴潮連

- `イラスト` 単体は**入れない**（公式の「Ver.3.5 ストーリーイラスト壁紙」が落ちる）
- `衣装` も**入れない**（ゲーム内の新衣装は正当なネタ）
- `交換コード` `ガチャ日程` `生放送` は**収集段階で落とさない**。
  「提案に出さない」と「情報が要らない」は別。生放送は台本の一次情報になる
- 誤検出1件を修正: 徳島の阿波踊りに『鳴潮連』という連があり、
  「鳴潮」の部分一致でゲームと無関係の記事を拾っていた

既存データ全1,615件に当てて検証。動画DBの該当20件は `purge_excluded.py --apply` で
アーカイブ済み（1,097行 → 1,077行、30日は復元可能）。

### 2-4. 沈黙していた情報源の発見と修復

**200以外のHTTPステータスを黙って読み飛ばしていた**ため、2つの情報源が
稼働開始から一度も収集できていないのに、ログ上は何も起きていないように見えていた。

- **Togetter**: URLが誤り（`/rss/tag/` は404、正しくは `/rss/t/`）。修復済み
- **TikTok**: RSSではなくJS描画のHTMLページ。取得手段が無いため `enabled:false`
- HTTPエラーと「取得できたのに0件」を区別して警告を出すよう修正

### 2-5. ピックアップ一覧のUI改善

- 戻り導線を両方（`/` と `/intelligence`）に設置
- 行のリスト表示 → **グリッド表示**（YouTubeトレンド収集と同じ見た目・列幅スライダー）
- **ホバー／画面中央での自動再生**を追加。`useHoverAutoplay` に切り出し、
  `VideoCard` の重複実装を削除
- 再生していない間は iframe を生成しないため、枚数が増えても重くならない

### 2-6. カレンダーへの競合タイトル追加

- `src/data/rival_schedule.json`（新規）と `GET /api/rival-schedule`（新規）
- **鳴潮の予定（`upcoming_schedule.json`）とは別ファイル**。混ぜると
  ネタのスコア計算 `schedule_boost()` が他ゲームのイベント名に反応する。
  また script_forge が同ファイルをローカルパス直指定で読んでいる
- 判明分はデータファイルに持ち、そこから先は更新周期で機械的に伸ばす（点線＝予測）
- **前半・後半**を分けて表示（後半は開始日からの逆算のため未確定扱い）
- **新キャラ実装の有無**を🆕で表示。読み取れない場合は何も出さない
  （false と「不明」を混ぜると「新キャラ無し」と断言することになる）
- 競合分は**専用の行**に描く。鳴潮と同じレーンに載せると単日ゆえ最後尾に回り、
  予定が多い週で「他N件」に畳まれて消える（実際に原神7.0が隠れた）
- **1日1回、5タイトルの攻略サイトから自動収集**（`update_rival_schedule_if_stale`）

オーナー方針により、**他タイトルに載せるのは「前半・後半」と「新キャラの有無」だけ**。
ガチャのキャラ名や開催期間は載せない（検討したが不要と判断された）。

### 2-7. 鳴潮カレンダーの表示修正

終了日を持たない予定（Ver3.6の8件）が幅1日で描かれ、名前が読めなかった。
カテゴリを問わず次バージョンの前日まで伸ばすようにした。
週をまたぐ帯にも各週で見出しを出す（従来は開始週のみ）。

---

## 3. 変更ファイル

### 新規

```
src/app/api/pickups/route.js                  ピックアップの取得・更新
src/app/api/rival-schedule/route.js           競合タイトルの日程
src/app/pickups/page.js                       ピックアップ一覧画面
src/app/pickups/page.module.css
src/app/pickups/PickupCard.js                 グリッド用カード
src/app/pickups/PickupCard.module.css
src/app/components/useHoverAutoplay.js        自動再生の共通処理
src/data/rival_schedule.json                  競合タイトルのデータ
docs/pickup-integration.md                    連携仕様と落とし穴
```

### 変更

```
scraper/intelligence_collector.py             +310行。重複チェック / 自動アーカイブ条件 /
                                              除外語 / 競合日程の自動収集
scraper/trend_collector.py                    動画DBに採用・制作状況を追加
scraper/config.json                           除外ワード 8語 → 24語
scraper/config_intelligence.json              rival_schedule_sources 追加、TikTok無効化、
                                              Togetter URL修正
src/app/api/videos/route.js                   adopted / status を返す
src/app/components/ScheduleCalendarModal.js   競合表示・前後半・帯の伸長
src/app/components/ScheduleCalendarModal.module.css
src/app/components/VideoCard.js               自動再生を共通処理へ移譲
src/app/intelligence/page.js                  ピックアップ一覧への導線
src/app/intelligence/page.module.css
src/app/page.js                               ピックアップ一覧への導線
docs/improvement-proposals.md                 他タイトルの扱い方針を記録
```

---

## 4. 実行したテストと結果

**単体テストは無い（このプロジェクトにテストコードが存在しない）。**
すべて実データ・実環境での確認。

| 対象 | 方法 | 結果 |
|---|---|---|
| ピックアップの通し | 本番URLで 採用付与 → 一覧表示 → 制作状況変更 → 一括解除 → Notion直接確認 | 全項目成功。`archived:false` / `採用:false` を確認 |
| 空データのPATCH | `{"ids":[]}` を送信 | 400 を返す |
| 自動アーカイブの条件 | 企画DB 1行を4状態に切り替え、掃除対象数を計測 | 見送り/制作中/投稿済み=守られる(517)、未着手=対象(518) |
| 除外語の影響 | 既存 1,595件に新旧フィルタを当てて差分を目視 | 救済10件 / 新たに落ちる0件 |
| 除外語の追加 | 全1,615件で新規除外分を1件ずつ目視 | 企画DB5件・動画DB20件、誤検出なし |
| 競合日程API | 生成された日付・前後半・バージョン番号を確認 | 53イベント。日付ずれ1件を発見し修正 |
| 競合日程の自動収集 | `stale_after_hours=0` で実行 | 5ソース取得成功、4〜5タイトル更新 |
| カレンダー描画 | ブラウザで実際に開き、帯の幅と表示件数を計測 | Ver3.6が3週にわたり全幅表示。競合5件が全て可視 |
| グリッド表示 | ブラウザで12枚表示・ホバー・列数変更 | iframe1つだけ生成、3列→4列に変化 |
| ビルド | `npx next build` | 成功（警告のみ、既存分） |
| Python | `py_compile` | 成功 |

### 途中で見つけて直した不具合

- 競合日程の生成が**1日ずつ手前にずれていた**（JST指定のDateを `toISOString()` に
  通すと9時間戻って前日になる）。UTC上で計算するよう修正
- 予備Geminiモデルの指定が誤り。`models.list()` に載っていても
  `no longer available to new users` で404になるものがある（`gemini-2.5-flash` 等）。
  実際に呼べることを確認した `gemini-flash-lite-latest` / `gemini-3.5-flash` に差し替え
- エンドフィールドの `version` にサブタイトルが入り「Ver拾遺散記」と表示された。
  数字ドット数字のみ通すよう検証を追加
- 自動収集が既存の確定日を上書きで消していた。統合方式に変更

---

## 5. 未確認事項・既知の問題

### 5-1. 原因未確定（優先度: 高）

**ピックアップの書き込みが15分間まったく届かなかった事象**（8/11 15:34〜15:50）。

- Notionの編集履歴で、その間の書き込みが4行のみだったことは確定
- 台本作成側で「書き込み失敗を握り潰していた」欠陥が実在し修正済み
- ただし**それが今回の症状の原因だったかは再現できていない**
- オーナーの証言も「2〜4本、正確には覚えていない」で確定していない
- **本アプリ側の受信・キャッシュは正常**であることは確認済み
  （`X-Vercel-Cache: MISS`、本番ブラウザで描画確認）

### 5-2. 情報源の取りこぼし

- **ゼンゼロ・エンドフィールドの新キャラ有無が「不明」のまま**。
  情報源のページに記載が無い。印を出さない仕様なので害は無いが、
  ゼンゼロ3.1には実際にはレミエール／シグリッドが実装される
- **エンドフィールド**は日によって1件も読み取れないことがある（既存データは維持される）

### 5-3. 環境要因

- **Gemini が 503 を頻繁に返す**（このセッション中に5回以上）。
  再試行と予備モデルで回避しているが、無料枠は1日20回のため注意
- 自動収集は1日1回（`rival_schedule_interval_hours: 24`）

### 5-4. 未追跡ファイル

`.agents/` と `skills-lock.json` が未追跡のまま残っている。
**このセッションで作成したものではない**ため触っていない。

---

## 6. Codex への依頼内容

### 6-0. 進め方（この順で進めること）

1. このHANDOFF.mdを読む
2. 現在のファイル状態を確認する（`git log --oneline -20`、下記の変更ファイル）
3. **レビュー結果と修正方針を説明する。ここで一度止まること**
4. オーナーの確認を得てから修正に入る
5. 修正後、実行したテストと変更内容を報告する

**いきなり修正には入らないこと。** 3の説明までが今回の依頼範囲。

### 6-1. 重点的に見てほしい箇所

1. **`src/app/api/pickups/route.js` の PATCH**
   - 3件ずつ350ms間隔でNotionへ送っているが、この密度で妥当か
   - 部分失敗時（`failed` が空でない）の扱い。現在は画面側で全体をロールバックしている
   - `linkedIds` を平坦化して送るため、同じページIDが重複しうる。実害の有無

2. **`scraper/intelligence_collector.py` の `update_rival_schedule_if_stale()`**
   - 統合ロジック（バージョン番号をキーに既存とマージ）。
     バージョン番号が取れなかった場合は日付をキーにしているが、
     日付が変更されたときに古い行が残らないか
   - 60日より古いエントリを落としているが、この閾値で問題ないか
   - 予備モデルへのフォールバック順序

3. **`src/app/components/ScheduleCalendarModal.js`**
   - `normalizedOwn` を `forEach` で破壊的に書き換えている（`ev._end` の代入）。
     元配列は `map` の結果なので影響は無いはずだが要確認
   - 週ごとに `rivalNormalized` を `filter` で走査しており、
     イベント数×週数のループになっている。件数が増えたときの負荷

4. **除外フィルタ全般（`is_relevant` / `should_exclude`）**
   - 残っている語に、`レイヤー ⊂ プレイヤー` と同種の部分一致事故が無いか。
     特に一般語である `落書き` `らくがき` `切り抜き` `クライシス` `マトリクス`
   - **注意**: `逆境深塔` `マトリクス` `クライシス` の3語は
     オーナーが意図的に入れている。外す提案はしないこと

5. **`useHoverAutoplay` への切り出し**
   - `VideoCard` から挙動が変わっていないか。特に Intersection Observer の
     クリーンアップ（元は `cardRef.current` を直接参照していた）

### 6-2. 見なくてよい箇所

- `src/data/upcoming_schedule.json`、`scraper/logs.json`、
  `scraper/intelligence_logs.json`、`src/data/intelligence_cache.json` は自動生成
- `docs/` 配下は記録用

### 6-3. 制約

- **Notion DBは本番。テスト目的で `採用` を書き換えないこと。**
  このセッションで一度、検証データの後片付け時にオーナーの登録分を
  巻き込んで消す事故を起こしている（復元済み）
- Vercel は無料枠1日100デプロイでアカウント共有。
  `scripts/vercel-ignore.sh` によりデータファイルのみの変更はビルドされない
- Gemini 無料枠は1日20回。デバッグで収集を繰り返し実行しないこと
- 台本作成側（`C:\Users\doras\Music\雑多\台本作成-Shorts`）が
  `src/data/upcoming_schedule.json` をローカルパス直指定で読んでいる。
  このファイルの形式を変えないこと

---

# 追記 (2026-08-20): 出稿中の広告クリエイティブの自動収集

- 対象コミット: `f80dfcc`
- 状態: **main にマージ済み**。本番での実収集は未実施（次回の巡回で自動的に走る）

## A-1. 作業目的

「鳴潮の広告動画も自動で収集したい」という依頼。
ユーザーの意図は**公式PVではなく、実際に出稿されている広告クリエイティブ**。

## A-2. 調査結果（実測）

| 経路 | 可否 | 根拠 |
|---|---|---|
| Google広告の透明性センター | **可** | 内部RPC `SearchService/SearchCreatives` に実リクエストを送り、日本向け90件・動画広告45件を取得できることを確認 |
| Meta広告ライブラリ | 不可 | 公式APIは非政治広告についてEU/UK限定。公開UIの内部エンドポイントは応答が空 |
| TikTok Creative Center | 不可 | `top_ads` APIは `40101 no permission`。匿名トークンの生成が別途必要 |
| X (Twitter) | 不可 | 広告透明性センターが既に廃止 |

Googleのみ実装した。鍵も認証も不要な公開データで、追加コストはゼロ。

### 判明した通信仕様（自前で観測。公式ドキュメントは存在しない）

```
POST https://adstransparency.google.com/anji/_/rpc/SearchService/SearchCreatives?authuser=
Content-Type: application/x-www-form-urlencoded

f.req={"2":40,"3":{"8":[2392],"12":{"1":"<ドメイン>","2":true}},"7":{"1":1,"2":0,"3":2392}}
```

- `2` = 1ページあたりの件数（上限40）／ `4` = 次ページのトークン（応答の `2`）
- `3.8` / `7.3` = 地域コード（日本 = 2392）
- `3.12.1` = 対象ドメイン。鳴潮の広告主アカウントは複数あり、名前検索では引けない
- 応答 `1[]` の各要素: `2`=クリエイティブID / `3.1.4`=プレビューURL / `4`=形式(1:画像 2:テキスト 3:動画) / `6`,`7`=初回・最終表示のUNIX秒 / `12`=広告主名
- 動画IDはプレビュー本体が難読化されているため、埋め込まれた `ytimg.com/vi/<11文字>` から取り出している

## A-3. 変更ファイル

| ファイル | 内容 |
|---|---|
| `scraper/ad_collector.py` | 新設。ページ送り・キャッシュ・期間の取り出し |
| `scraper/trend_collector.py` | ステップ2.5を追加。`collect_ad_videos_safely()` で本体から隔離。Notionへカテゴリ「広告」で送信。列「出稿期間」を自動追加 |
| `scraper/config.json` | `ad_transparency` セクションを追加。**公式チャンネルIDの死活修正**（後述） |
| `scraper/tests/test_ad_collector.py` | 新設。9件 |
| `src/app/page.js` | タブ「広告」を追加 |
| `src/app/api/videos/route.js` | `adPeriod` を返す |
| `src/app/components/VideoCard.js` / `.module.css` | 出稿期間バッジ |
| `.github/workflows/update_trends.yml` | `ad_creative_cache.json` を持ち帰る |

## A-4. あわせて直した不具合

`config.json` の `target_channels` にあった公式チャンネルID `UCw-Oq6UqC_U9o1mI1g4dIxg`
は**現在404で存在しない**。RSSで両方に実リクエストを送って確認済み。
正しいIDは `UCGc93NguHRwzv1Rw9MyIcxQ`（@wutheringwaves3352）。

`fetch_channel_latest_videos()` は取得失敗を空リストで返すため、
**公式チャンネルからの収集は一度も成立していなかったが、誰も気づけなかった。**

## A-5. テスト結果

```
lint          exit=0（警告4件・すべて既存）
JSテスト      11件 OK
Pythonテスト  34件 OK（新規9件）
build         exit=0
実収集ドライラン  広告90件確認 → 動画広告45件回収（動画以外20 / 除外0）
キャッシュ再実行  65件すべてキャッシュ命中・1.3秒
```

## A-6. 未確認事項

1. **本番での実収集が未実施。** ローカルに `YOUTUBE_API_KEY` が無く、
   再生数が0のまま登録されると二度と埋まらないため、あえて流していない。
   次回の巡回で自動的に走る。
2. 再生数の付与は `YouTubeKeyManager` を新規に生成している。
   本体の検索でキー#1を使い切っていた場合、広告側でもう一度ローテーションが走る。
   `execute()` が処理するので破綻はしないが、無駄ではある。
3. 透明性センターの仕様変更検知は「0件ならログに警告」まで。Discord通知には繋いでいない。

## A-7. Codexへの依頼

1. まず本ファイルと `scraper/ad_collector.py` を読み、現状を確認してほしい。
2. その上で、以下の観点でレビュー結果と修正方針を説明してほしい（**先に修正はしないこと**）。
   - 内部RPCへの依存の妥当性と、壊れたときの検知方法
   - `collect_ad_videos_safely()` の隔離が本当に漏れなく効いているか
   - キャッシュが無限に増え続ける点（古いクリエイティブの掃除が無い）
   - A-6の2番（キーマネージャの二重生成）の扱い
3. 内容を確認してから修正を依頼する。

### 触らないでほしいもの（前回から継続）

- 未追跡の `.agents/`、`.claude/`、`skills-lock.json`
- `src/data/upcoming_schedule.json` の形式（script_forge がローカルパス直指定で読む）
- 除外ワード（逆境深塔・マトリクス・クライシスは**意図的**に入れている）
- 本番Notionを自動テストに使わないこと。既存の採用・制作状況を検証目的で変更しないこと

---

# 追記2 (2026-08-21): 広告収集の全面やり直し

- 対象コミット: `56f8524`
- 状態: **main にマージ済み**。本番の実収集は次回巡回で走る

## B-1. 前回報告の誤り

追記1で「動画広告45件を回収」と報告したが、実態は次のとおりだった。

1. **45件すべてが既にDBに存在していた。** 新規行はゼロ。カテゴリ「広告」は
   永久に空のままだった（URL重複で `send_to_notion` が全件スキップする）
2. **絞り込みが狭すぎて9割以上を取りこぼしていた。** 正しく引くと742件

45件中42件が公式チャンネルの通常投稿であることは、報告前の集計出力に
出ていた。それを確認せず、残る3件の珍しい例を見出しに置いて報告した。

## B-2. 取りこぼしの原因（2つとも絞り込みの誤り）

| 誤り | 正しくは | 影響 |
|---|---|---|
| 遷移先ドメインで引いていた | 広告主IDで引く | 代理店3社分が丸ごと落ちる |
| 地域を日本に固定していた | 地域を絞らない | 海外向け素材が落ちる |

実測: ドメイン+日本 = 45件 → 広告主ID+地域なし = **742件**（うちDB未登録690件）

## B-3. 本命だったもの

広告素材の多くは **限定公開のチャンネル `UCAMjmQpxCVKhYdRCv4tYajA`** にある。

```
RSS (feeds/videos.xml?channel_id=UCAMjmQpxCVKhYdRCv4tYajA) → 200 だが entry 0件
動画ページの isUnlisted → true
```

検索にもRSSにも出ないため、**透明性センター以外に発見手段が無い。** ここに
223件ある。ファイル名がそのまま素材の説明になっている。

```
WW-JP-V-ZJY-BB-3.1-小爱直播間无UI-1080x1920.mp4       配信画面を模した縦型広告
WW-KR-V-SSW-SSW-3.6-仿KOL合軸看劇情-1920x1080.mp4     KOL風
WW-JP-V-SSW-DJS-3.6-清宵直面BOSS拼主城行走-1920x1080.mp4
```

## B-4. 変更内容

| 箇所 | 内容 |
|---|---|
| `ad_collector.py` | 広告主IDの自動洗い出し（代理店の増減に追従／一度見つけたIDは控えに残す）、地域指定の廃止、プレビュー取得の並列化、取りこぼしの再試行、PGR除外 |
| `trend_collector.py` | `apply_ad_periods()` で既存行に出稿期間を書き足す。対象取得のページ送り漏れを修正 |
| `src/app/api/videos/route.js` | `?ads=1` で出稿期間ありだけを最大2000件取得 |
| `src/app/page.js` | 広告タブを別取得に。通常一覧の500件枠に収まらないため |
| `tests/test_ad_collector.py` | 9件 → 19件 |

### 特に注意して読んでほしい設計判断

`_needs_resolving()` — プレビューは毎回組み立て直され、同じ広告でも動画IDが
入らない回がある（実測1398件中361件）。1回の空振りで「動画ではない広告」と
確定させると永久に埋もれるため、回数を区切って引き直している。

## B-5. テスト結果

```
lint          exit=0（警告4件・すべて既存）
JSテスト      11件 OK
Pythonテスト  44件 OK
build         exit=0
実収集        広告主4件 / クリエイティブ1677件 / 動画広告742件 / 別ゲーム除外10件
DB突合        742件中 690件がDB未登録
```

## B-6. 未確認・未対応

1. **次回巡回で約690行がNotionに追加される。** 海外向けを含む方針はユーザー承認済み
2. **画像・テキスト広告553件は捨てている。** 動画DBに入らないため。
   透明性センター上の閲覧URL (`/advertiser/<AR>/creative/<CR>`) は永続なので、
   拾う余地はある
3. **Meta広告ライブラリは公開されており閲覧できる（追記1の「不可」は誤り）。**
   ただし鳴潮/Wuthering Waves で引くと出るのは転売・課金代行など第三者の広告で、
   Kuro Games自身の出稿は特定できていない。未認証の自動取得は400で弾かれる
4. TikTok Creative Center は `40101 no permission` のまま
5. 広告主の洗い出しは各ドメイン20ページまで。それ以深にしか出ない代理店は
   初回で漏れる（控えに蓄積するので回を重ねれば拾う）

## B-7. Codexへの依頼

1. 本ファイルの追記2と `scraper/ad_collector.py` を読み、現状を確認してほしい。
2. その上でレビュー結果と修正方針を説明してほしい（**先に修正はしないこと**）。
   - 内部RPCへの依存と、壊れたときの検知（現状はログ警告のみ）
   - キャッシュが増え続ける点（1677件・掃除の仕組みが無い）
   - `apply_ad_periods` が既存行の最終更新日時を動かす点の是非
   - B-6の2番（画像広告の扱い）をどうするか

---

# 追記3 (2026-08-21): 本番での実収集が完了・確定値

## C-1. 結果（2026/08/21 06:01 の巡回、所要13分）

```
広告 1398 件を確認し、動画広告 660 件を回収（動画以外 360 件 / 除外 1 件）
カテゴリ「広告」: 新規追加 608 件 / スキップ 52 件 (登録済)
出稿期間を 7 件に記録しました（対象 660 件 / 変更なし 653 件）
```

Kuro本家(`广州库洛科技有限公司`)のみ。代理店3社の除外も動作した。
**DBに608件が新規で入り、画面の「広告」タブで見られる状態。**

所要13分は初回のプレビュー確認（約1400件）によるもの。以降はキャッシュが
効くため通常の1分程度に戻る想定。次回の実行時間で要確認。

## C-2. 未対応のまま残しているもの（ユーザー判断で後回し）

**CI (`ci.yml`) が `npm ci` で失敗し続けている。** 広告の作業とは無関係の
既存問題で、8/20以前から発生していた。

```
`npm ci` can only install packages when your package.json and
package-lock.json are in sync
Invalid: lock file's @emnapi/wasi-threads@1.2.1 does not satisfy 1.2.3
Missing: @emnapi/core@1.10.0 from lock file
```

`package-lock.json` が実際の依存関係とずれている。`npm install` で作り直して
コミットすれば直るが、依存バージョンが範囲内で動く可能性があるためユーザーの
判断待ち。**収集そのものには影響しないが、放置すると本当の不具合に気づけない。**

### Codexへ: この件を触る場合

1. `npm ci --dry-run` で再現を確認してから着手すること
2. `npm install` 後、`git diff package-lock.json` の変更が推移的依存の記録
   だけに収まっているかを確認し、`next`/`react` のバージョンが動いていないこと
3. その上で `npm run check` が通ることを確認する

---

# 追記4 (2026-08-21): 本家チャンネルのみに限定・掃除完了

## D-1. 追加で判明したこと

本家の広告アカウント (`广州库洛科技有限公司`) 自身が、**個人配信者の動画を
大量に広告として回していた**。広告主単位の絞り込みでは落とせない。

実データ742本を全数調査した結果、本家の素材は360本、残る382本(117チャンネル)は
配信者の動画だった。

## D-2. チャンネル単位の絞り込み

判定は**チャンネル名ではなくハンドル**で行う。名前で見ると誤判定する。
検証中に実際に踏んだ2件:

- 英語圏公式と同名の「Wuthering Waves」を名乗るチャンネルが別に存在する
- 配信者 `Blurred` のハンドルが `@buledeblurred_official`

```
@ww-op1vs                    173  広告専用の限定公開チャンネル
@wutheringwaves               52  英語圏公式  ← 一度取りこぼしかけた
@wutheringwaves3352           47  日本公式
@ww_kr_official               46  韓国公式
@wutheringwaves.zh.official   42  中国語公式
```

`@WUWACreatorHub` (6本) は公式運営らしき名前だが中身は配信者コンテンツのため
**除外している**。本家扱いにするなら config.json の `official_channels` に足す。

## D-3. 既存行の掃除（実施済み）

`scraper/purge_nonofficial_ads.py` を新設し、実行した。

```
カテゴリ「広告」644件 → 293件をゴミ箱へ移動（保護0 / 判定不能0 / 失敗0）
残り351件はすべて本家チャンネルの素材
```

採用済み・制作中・投稿済み・見送りの行は保護する。判定できなかった行も触らない。
移動記録は `scraper/purge_ads_audit.log`（gitignore済み）。Notionのゴミ箱から
30日間は復元可能。

## D-4. 画面

並べ替えに「出稿の新しい順(終了日)」「出稿の古い順(開始日)」を追加。
広告はまとめて登録されるため収集日では並ばない。出稿期間は
`2026-07-31 〜 2026-08-03` の形で、日付が YYYY-MM-DD なので文字列比較で
日付順になる。

## D-5. 状態

```
lint 0エラー / JSテスト 11件 / Pythonテスト 57件 / build 成功
```

未対応のまま残っているものは追記3の C-2（CI の `npm ci` 失敗）と、
画像・テキスト広告を拾っていない点。いずれもユーザー判断で後回し。

---

# 追記5 (2026-08-27): YouTube APIクォータを2本ぶんに収めた

## E-1. 作業目的

APIキーを4本から2本へ減らすにあたり、消費量を上限内に収める。

## E-2. 実測した消費量（変更前）

コードから数え、ログの枯渇パターンとも一致することを確認した。

| 呼び出し | 単価 | 回数/巡回 | 単位 |
|---|---:|---:|---:|
| `search.list` | 100 | 16 | 1,600 |
| `videos.list`（再生数） | 1 | 約16 | 16 |
| `playlistItems.list`（登録CH） | 1 | CH数 | 数 |
| **合計** | | | **約1,620** |

`× 24回/日 = 約38,900単位`（4キー＝4プロジェクトで40,000。ほぼ上限）

裏付け: 割り当てのリセットは16:00 JST（米国太平洋時間の0時）。ログ上、
21:00に1枚目、翌05:00に2枚目、13:00に3枚目が枯れていた。

## E-3. 変更内容と効果

**コードを送らずに、アプリの `/api/config` 経由で `scraper/config.json` を
書き換えた**（GitHubへの書き込みはアプリのトークンが行うため、後述の
Git認証の問題を回避できる）。

| 設定 | 変更前 | 変更後 |
|---|---:|---:|
| `shorts_ratio` | 0.85 | **1.0** |
| `search_queries` | 4語 | **3語**（「鳴潮 最新情報」を削除） |

```
約1,620単位/回 → 約620単位/回
約38,900単位/日 → 約14,900単位/日（2キー20,000の74%）
```

### なぜ shorts_ratio で検索が丸ごと消えるのか

`long_limit = max_results - int(max_results * shorts_ratio)` が 0 になり、
`fetch_youtube_api()` が [trend_collector.py:202](scraper/trend_collector.py:202) の
`if max_results <= 0: return []` で API を呼ばずに返るため。
1回100単位の検索が8回まるごと消える。

### 効かない操作（試さないこと）

**`max_results_per_query` を減らしても1単位も減らない。** `search.list` は
取得件数に関係なく一律100単位。

## E-4. 検証結果（本番実測）

```
2026/08/27 16:01 | Success | 新着16件
APIキー : Key #1 (メイン構成運用)   ← 上限警告なし。1本目だけで完走
検索設定: キーワード 3個            ← 反映確認
最新 (Shorts) 新規14件 / 週間人気 (Shorts) 新規2件 / 広告1,420件確認
```

変更前は毎回「Key #N にて1日上限に到達」が出ていたが、消えた。

## E-5. 重要な制約：クォータはプロジェクト単位

**キー単位ではなく Google Cloud プロジェクト単位で 10,000単位/日。**
同じプロジェクトで鍵を何本作っても合計10,000のまま。

そして[YouTube API利用規約](https://developers.google.com/youtube/terms/api-services-terms-of-service)は
「使用制限や割り当て制限を超過または回避してはならない」と定めており、
**割り当てを増やす目的で複数プロジェクトを作る行為は禁止**。運用上の罰則は
該当プロジェクトだけでなく**アカウントの全プロジェクト停止**。

現構成は別アカウント2つ＝別プロジェクト2つ（ユーザー確認済み）。
4本構成だった時点で既にこの領域にあり、2本化で露出は半減した。

### 目指す状態

**1プロジェクト（10,000単位）で足りる量まで下げる**のが規約上いちばん素直。
目標は約6,000単位/日。手段は次の3つ。

1. 週間人気の検索を1日4回程度に間引く（**要コード反映**）
2. 検索キーワードを3→2語に（設定のみで可能）
3. 通常尺は登録チャンネルで補う（`playlistItems` は1チャンネル1単位）

## E-6. 現在ブロックされているもの

### Git への書き込みができない

```
remote: Permission to diawork2000-tech/wuthering-trends-web.git denied to diasensei2000.
```

Windows資格情報マネージャーに別アカウントが保存されている。

```
Target: git:https://github.com          User: diasensei2000
Target: gh:github.com:diasensei2000     User: diasensei2000
```

**未送信のコミットが1件ある: `f1dedaf`（`.env.example` の追加）。**
動作には影響しない。

解消するには `gh auth login`（対話式のためClaudeからは実行不可）で
push権限のあるアカウントに入り直し、「Authenticate Git with your GitHub
credentials?」で **Yes** を選ぶ。`cmdkey /delete:LegacyGeneric:target=git:https://github.com`
でも直るが `gh` の認証は直らない。

**`gh` が通れば Claude 側から `gh workflow run` で収集を即時起動できる**
（現在は毎時の巡回を待つしかない）。

### CI が `npm ci` で失敗し続けている（ユーザー判断で後回し）

`package-lock.json` が実際の依存関係とずれている。追記3の C-2 参照。
**設定を変えるたびにCI失敗メールが飛ぶ**（`scraper/config.json` は
`paths-ignore` に入っていないため）。収集には影響しない。

直すには `npm install` → `git diff package-lock.json` が推移的依存の記録
だけに収まっていることを確認 → `npm run check` が通ることを確認。

## E-7. 環境変数の置き場（今回整理した）

| 場所 | 用途 | YOUTUBE_API_KEY |
|---|---|---|
| `scraper/.env` | 手元での動作確認 | **記入済み**（gitignore済み） |
| GitHub Secrets | 本番（Actions） | **2本に差し替え済み** |
| `.env.example` | 必要な変数の一覧（値なし） | 追加したが未送信 |

本番で動くのは Actions 側だけなので、手元の `.env` は確認用。

## E-8. 未対応のまま残しているもの

1. 1プロジェクト化（約6,000単位）— コード反映が必要
2. 通常尺の検索を間引いて復活させる — 同上
3. 画像・テキスト広告 351件を拾っていない（動画DBに入らないため）
4. X(Twitter)の広告 — **取得手段なし**。DSA広告リポジトリはEU配信分のみ、
   X Ads APIは自社キャンペーン管理用で競合広告は見られない。有料サービスも
   日本のX広告のカバーが不明。ユーザー判断で「無料のまま様子見」

## E-9. Codexへの依頼

1. 本ファイルの追記5と `scraper/config.json` を読み、現状を確認してほしい。
2. その上でレビュー結果と方針を説明してほしい（**先に修正はしないこと**）。
   - E-5の「1プロジェクト化」の実現方法として、間引きの実装方式
     （既存の `rival_schedule_interval_hours` と同じく前回実行からの経過を見る形）が妥当か
   - `get_youtube_trends()` が呼ばれるたびに `YouTubeKeyManager()` を新規生成し、
     毎回キー#1から再試行している点（クォータは消費しないが無駄がある）
   - E-6のCI不具合の直し方

---

# 追記6 (2026-08-27): 公式Twitter/BiliBiliの「地の投稿」が未収集（オーナー要望・要相談）

## F-1. 経緯

別チャット（YouTube分析・マーケティング相談）でオーナーと投稿頻度を検討していたところ、
「絶えず供給される公式ネタ（主に公式アカウントからの供給）の収集に困っている」という
指摘があった。`scraper/config.json` を確認したところ、現状の収集対象は

- YouTube（キーワード検索3語 + 公式チャンネル1つ）
- Google広告透明性センター（出稿中の広告クリエイティブ。追記1〜4参照）

**のみで、公式Twitter/X・公式BiliBiliの「地の投稿」（広告ではない、通常のツイート・
動画投稿そのもの）は一切収集していない。** E-8の「X(Twitter)の広告 — 取得手段なし」は
あくまで**広告**の話であり、公式アカウントの通常投稿とは別の話なので注意。

オーナーいわく、公式の一次情報はYouTubeより先にTwitter/BiliBiliに出ることが多いとのこと。

## F-2. このセッションで検討した方向性（未実装・提案のみ）

有料のX API（Basic以上）を使わずに済む方法として、**RSSHub**
（OSSのRSS生成プロキシ。Twitter/X・BiliBiliの両方にルートがある）を挟んで、
`trend_collector.py` に第3のソースとして足す案を検討した。

- 公式アカウントの新規ツイート／投稿をRSS化 → 既存のYouTube検索と同じ扱いでNotionに合流
- 追加の有料契約は不要（RSSHubの公開インスタンス、または自前ホストのどちらか）

## F-3. 未検証・要相談（ここが本題）

**このセッションでは技術調査のみで、実装はしていない。** 次に着手する前に、
**オーナーと以下を相談してから進めること**（いきなり実装しない）。

1. **RSSHubの安定性**: 公開インスタンスはX/BiliBili側の仕様変更で頻繁に壊れる。
   自前ホスト（Vercel上の別関数 or 既存プロジェクトへの追加）にするかの判断が要る
2. **対象アカウント**: 公式Twitter/BiliBiliの具体的なアカウント名・IDをオーナーから確認する
   （このセッションでは特定していない）
3. **収集頻度とコスト**: 既存の1時間おき巡回に相乗りするか、別サイクルにするか
4. **E-5で進行中のYouTube APIクォータ削減作業との優先順位**: そちらが先か、
   こちらを並行して進めてよいか

## F-4. Codex/次のセッションへの依頼

1. 本追記6と `scraper/config.json`・`scraper/trend_collector.py` を読む
2. RSSHub導入の実現方式(自前ホスト vs 公開インスタンス)について選択肢を整理し、
   **オーナーに提示して相談する**（先に実装しない）
3. 対象アカウントをオーナーに確認する
4. 合意が取れてから実装に入る

---

# 追記7 (2026-08-29): 公式SNSの投稿収集を実装・稼働開始（BiliBili/Weiboのみ未解決）

追記6で「相談してから進めること」としていた件を、オーナーの承認を得て実装し、
本番で稼働させた。**Codexがしばらく代理で担当する可能性があるため、
判断の根拠と踏んだ落とし穴を残す。**

## G-1. 何が動いているか（2026-08-29 20:01 時点の実測）

```
📡 公式SNS 対象 14 アカウント中 11 件から投稿 193 件を回収
   カテゴリ「SNS」: 新規追加 4 件 / スキップ 189 件
```

| 媒体 | 件数 | 状態 |
|---|---|---|
| X（日本語6・英語2・韓国語2） | 10 | 正常。毎時4件前後の新着 |
| Reddit（r/WutheringWaves） | 1 | 正常。RSSHubを介さない |
| BiliBili 2件・Weibo 1件 | 3 | **未解決**（G-5参照） |

画面側は動画一覧に「SNS」タブを新設した。媒体・言語・アカウント・未読で
絞り込め、投稿日時で並べ替えられる。中国語・韓国語は日本語に訳し、原文は
ボタンで確認できる。収集対象のアカウント一覧も画面から見られる。

## G-2. 追加・変更したファイル

| ファイル | 役割 |
|---|---|
| `scraper/sns_collector.py` | 新規。収集の本体 |
| `scraper/official_accounts.json` | 新規。45アカウントの一覧。`collect: true` の14件が対象 |
| `scraper/purge_sns_posts.py` | 新規。媒体を指定して行をゴミ箱へ移す（作りを直した後の入れ替え用） |
| `scraper/tests/test_sns_collector.py` | 新規。34件。落とし穴を再発させないための番人 |
| `src/app/api/media/route.js` | 新規。Twitterの動画を中継する（G-4-3参照） |
| `scraper/trend_collector.py` | ステップ2.6を追加。Notionの列追加、翻訳の呼び出し |
| `src/app/page.js` / `VideoCard.js` とCSS | SNSタブ・絞り込み・⭐・原文表示 |
| `.github/workflows/update_trends.yml` | RSSHubの接続情報を渡す2行 |
| `docs/railway-rsshub-setup.html` | RSSHub設置の手順書（オーナー向け） |

**アカウントの増減はコードではなく `official_accounts.json` で行う。**
画面側も同じファイルを読んでいるので、片方だけ直すと表示と実収集がズレる。

## G-3. 構成（どこで何が動くか）

```
GitHub Actions（毎時02分）──収集──> Notion ──表示──> Vercel
       │
       └──> Railway の RSSHub ──> X / BiliBili / Weibo
            （Reddit だけは直接読むので、RSSHubが落ちても生き残る）
```

- RSSHubは**自前ホストが必須**。公開インスタンス(rsshub.app)は自動アクセスを
  Cloudflareで丸ごと拒否する（実測で確認済み）
- 接続情報は GitHub Secrets の `RSSHUB_BASE_URL` / `RSSHUB_ACCESS_KEY`
- Railway側の変数: `TWITTER_AUTH_TOKEN`, `ACCESS_KEY`, `NODE_ENV`,
  `PLAYWRIGHT_BROWSERS_PATH`
- 費用は実測で**月$2.7程度**（メモリ193MB × $10/GB/月 が主）。上限1000MBで
  頭打ちなので、最悪でも月$10を超えない

## G-4. 踏んだ落とし穴（同じ轍を踏まないこと）

### G-4-1. 「HTTP 200 なのに 0 件」が最も危険

TikTokは常にこの形で返す。`config_intelligence.json` にも過去の無効化メモが
残っており、稼働開始から一度も取れていなかった。エラーで止まらないため
「正常終了・0件」が延々と続き、取り逃しに気づけない。

**0件と失敗は必ず数えてログに出すこと。** 対象数と取得数を混ぜて
「14アカウントから20件」と書くと、13件見送られていても全部から取れたように
読める。実際に一度そう書いて指摘を受けた。

### G-4-2. 翻訳は黙って失敗する

`translate_if_needed` は例外を握りつぶして原文を返す。そのため

- 毎時の巡回で取り直した投稿を丸ごと訳し、1回で百件近く呼んで弾かれ、
  **一度も訳せていなかった**（ハングルのまま39件並んでいた）
- 翻訳先が落ちたときの**エラー画面の文面がタイトルとして7件登録されていた**
  （`Error 500 (Server Error)!!1500.That's an error`）

対処として、訳すのは**Notionに未登録の投稿だけ**に絞り、エラー画面と判別
できる文面は失敗として扱い、失敗件数をログに出すようにした。

### G-4-3. Twitterの画像と動画は挙動が違う

| | 他サイトからの読み込み |
|---|---|
| 画像 `pbs.twimg.com` | **通る** |
| 動画 `video.twimg.com` | **参照元(Referer)を見て403で拒否** |

実測:
```
参照元なし     → 200 / video/mp4 / 2.3MB（範囲指定も206）
参照元を付ける → 403 / 0 bytes
```

そのため `src/app/api/media/route.js` で中継している。中継先は
`video.twimg.com` だけに絞ってある。**絞りを外すと踏み台になる。**

さらに、この中継で二次被害を出した。範囲指定の応答を共有の置き場に
キャッシュさせたため、**動画要素が末尾を読みに来たときの応答（末尾10KB）が
居座り、以後どの読み込みにも返って再生できなくなった。** `Vary: Range` と
`private` で解決。中継の応答を作り替えたときは `VideoCard.js` の
`/api/media?v=2` の数字を上げて、古い応答を掴んでいるブラウザを救うこと。

### G-4-4. HTMLから取り出したURLは実体参照を戻す

本文はHTMLなので `&` が `&amp;` と書かれている。戻さずに使い、
`?format=jpg&amp;name=orig` となって**186件の画像が404**になっていた。
`_clean_url()` を必ず通すこと。

### G-4-5. GitHub Secrets はワークフローに書かないと渡らない

オーナーが正しく登録していたのに、`update_trends.yml` の `env:` に
並べ忘れていたため、13アカウントが「RSSHub未設定」として見送られ続けた。
**環境変数を増やしたらワークフロー側も直す。**

## G-5. 未解決：BiliBili と Weibo が HTTP 503

**この3件だけが残っている。** 原因は判明済みで、RSSHubがブラウザを
起動できないこと。BiliBiliの動態もWeiboもブラウザ描画を必要とする
（`weibo/user.ts` は `requirePuppeteer: true`）。

```
Error: browserType.launch: Executable doesn't exist at
  /app/node_modules/.cache/ms-playwright/chromium_headless_shell-1234/
  chrome-headless-shell-linux64/chrome-headless-shell
```

`PLAYWRIGHT_BROWSERS_PATH` を追加して探す場所は正しくなったが、そこに
**軽量版ブラウザの実体が無い**。RSSHubのDockerfileを読んだ限り、

```dockerfile
RUN echo "CHROMIUM_EXECUTABLE_PATH=$_chrome_path" | tee /app/.env
COPY --from=docker-minifier /app /app    # ← 直後に /app を丸ごと上書き
```

書き込んだ設定が次の行で消える作りになっており、**イメージ側の問題**。

**次に試すこと（この順で）**

1. Railwayの変数に `CHROMIUM_EXECUTABLE_PATH` を追加。値は
   `/app/node_modules/.cache/ms-playwright/chromium-1234/chrome-linux/chrome`
   （`1234` はエラーの版数から推測。**外れる可能性がある**）
2. 外れたらRailwayのコンソールで `ls /app/node_modules/.cache/ms-playwright`
   を実行し、実際のフォルダ名からパスを組む
   （※オーナーはコンソールにコピペできないため、短いコマンドにすること）
3. それも駄目なら `browserless/chrome` を別サービスとして立て、
   `PLAYWRIGHT_WS_ENDPOINT` で繋ぐ。RSSHub公式の docker-compose と同じ構成。
   確実だが月$1〜2増える。**費用が増えるので必ずオーナーの承認を取ること**

**急ぎではない。** 日本語の一次情報はX側で揃っており、オーナーからも
「後回しでも支障ない」との認識を共有済み。

## G-6. 触ってはいけないもの・注意

- **未追跡の `.agents/`, `.claude/`, `skills-lock.json` には触らない**
- 除外ワード（逆境深塔・マトリクス・クライシス）は**意図的**。外さない
- **削除は必ずオーナーの確認を取る。** `purge_sns_posts.py` は既定が下見で、
  `--apply` を付けたときだけ動く。採用済み・制作中などの行は保護される
- 本番Notionを自動テストに使わない
- CIの `npm ci` は `package-lock.json` のズレで失敗し続けている。**既知・保留**。
  設定変更のたびにエラーメールが飛ぶが、オーナーは「今回はいいです」との判断
- YouTubeクォータは2本で1日約14,900/20,000。**手動実行1回で約620消費する。**
  検証は原則、毎時の定期巡回の結果で行うこと

## G-7. 作業の進め方（オーナーから受けた指摘）

このセッションで実際に叱責を受けた点。**代理で入る場合も同じ基準で。**

1. **確かめずに報告しない。** 「動くはず」で報告して2度手戻りした。
   ブラウザから実際に触れる手段があるなら、自分で確認してから報告する
2. **費用が発生する提案は、単価と概算を先に出す。** 「まず数日動かして実測を」
   と言って課金を先行させ、不信を招いた
3. **数字を混ぜない。** 対象数と成功数、書き込み数と変更数を分けて出す
4. **初心者向けに書く。** git用語（commit/push）は通じない。結果で説明する
5. 手順はチャットに流さずHTMLファイルにする（`docs/` 配下）

## G-8. 検証コマンド

```bash
npm run build          # 通ること
npm run lint           # エラー0（警告4件は既存）
cd scraper && python -m pytest tests/ -q   # 91件 通過
```

## G-9. Codexへの依頼

1. 本追記7と `scraper/sns_collector.py` を読み、現状を把握する
2. G-5の1を試すかどうか、**オーナーに提示して判断を仰ぐ**
   （費用が増える3を選ぶ場合は必ず承認を取る）
3. 手を入れる前に G-4 を読むこと。**同じ落とし穴を再度踏まないため、
   テストが34件用意してある。消さないこと**
